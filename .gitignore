# ---- Secrets : ne JAMAIS committer ----------------------------------------
.env
.env.*
!.env.example
*.pem
*.pfx
*.key

# ---- Environnements virtuels ----------------------------------------------
venv/
.venv/
env/

# ---- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# ---- Outils ---------------------------------------------------------------
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ---- Éditeurs / OS --------------------------------------------------------
.vscode/
.idea/
Thumbs.db
desktop.ini

# Documents internes Teknologiia : utiles localement, mais un depot public
# n'est pas le bon endroit pour la specification interne d'un employeur.
*.pdf
!docs/*.pdf

# ---- Journal d'audit ------------------------------------------------------
# Les dossiers d'investigation portent des UPN, des adresses IP et des
# verdicts : de la telemetrie de tenant, qui n'a rien a faire dans un depot.
data/
rapport-evaluation.json

# ---- ARGUS Web : Java + React ---------------------------------------------
argus-web/backend/target/
argus-web/frontend/node_modules/
argus-web/frontend/dist/
# Sortie de construction du front, recopiee dans le backend par `npm run build`.
# La versionner produirait un diff a chaque build, sur des noms de fichiers
# haches qui changent a chaque fois.
argus-web/backend/src/main/resources/static/
.mvn/wrapper/maven-wrapper.jar
